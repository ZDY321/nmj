# 项目通览优化执行清单

> 创建时间：2026-07-13  
> 目标：按优先级处理项目中的数据一致性、安全、性能、测试和可维护性问题。  
> 使用方式：后续实施时更新每项状态，并在条目末尾记录完成日期、涉及文件和验证结果。

## 状态说明

- `[ ]` 待处理
- `[~]` 处理中
- `[x]` 已完成
- `[!]` 阻塞

## 当前基线

- 生产构建：通过
- 自动测试：7 个测试文件，41 项测试全部通过
- TypeScript：严格模式
- 当前工作区：检查时无未提交修改
- 依赖审计：8 项已知漏洞，其中 6 项高危
- 主要模块规模：
  - `src/frontend/views/ScheduleView.tsx`：约 3390 行
  - `src/frontend/App.tsx`：约 2130 行
  - `src/frontend/views/StudentsView.tsx`：约 2050 行
  - `src/worker/index.ts`：约 2200 行

## P0：数据一致性

### [x] P0.1 修复学习清单批量删除相互覆盖

完成记录：2026-07-13。新增批量删除 mutation，并使用最新 vault 引用处理同一次交互中的连续更新；完整测试通过。

问题：

- `ProgressChecklistView` 清除一个单元格的多条完成记录时，会循环调用单条删除。
- `App.updateVault` 可能在同一次交互中基于同一份旧 `vault` 生成多个保存结果。
- 最终可能只删除最后一条记录，前面的删除被后续整库保存覆盖。

涉及文件：

- `src/frontend/views/ProgressChecklistView.tsx`
- `src/frontend/App.tsx`

建议方案：

- 新增批量删除完成记录的 mutation。
- 避免在一次用户操作中循环调用单条 `updateVault`。
- 将 vault 更新改造成可靠的函数式更新或串行 mutation 队列。

验收：

- 一个单元格存在多条完成记录时，点击清除可一次性全部删除。
- 连续批量删除不会恢复前一条已删除记录。
- 增加对应回归测试。

### [x] P0.2 修复恢复补课课节后的原课状态

完成记录：2026-07-13。恢复课节后重新同步原课补课状态并重算关联费用快照；新增回归测试。

问题：

- 删除已完成补课时，系统会把原课学生恢复为待补状态。
- 从回收站恢复该补课课节时，目前只把课节放回活动列表，没有重新同步原课。
- 可能出现补课课节已经完成，但原课仍显示待补、对账也未计入有效实到。

涉及文件：

- `src/frontend/App.tsx`
- `src/frontend/lib/vaultMutations.ts`

建议方案：

- 恢复课节后调用补课完成同步逻辑。
- 重算原课及关联补课课节费用快照。
- 批量恢复时统一收集受影响的原课 ID 后处理。

验收：

- 恢复已完成补课后，原课对应学生重新显示为已补课。
- 教务对账重新计入有效实到，并保留“已补课”标识。
- 删除再恢复未完成补课时，原课保持待补状态。
- 增加已完成和未完成补课的恢复测试。

### [x] P0.3 防止删除原课后留下孤立补课

完成记录：2026-07-13。删除原课时关联补课一并进入回收站；恢复时校验原课关系；永久删除原课时同步清理回收站关联补课。

问题：

- 当前删除补课课节时会更新原课。
- 删除原课时，没有明确处理仍引用该原课的补课课节。
- 原课被永久删除后，补课课节可能继续引用不存在的 `linkedOriginalLessonId`。

涉及文件：

- `src/frontend/lib/vaultMutations.ts`
- `src/frontend/views/ScheduleView.tsx`
- `src/frontend/App.tsx`

建议方案：

- 删除原课前检查关联补课课节。
- 提供明确策略：连同补课移入回收站，或解除补课关联。
- 永久删除回收站原课前再次检查活动课节和回收站中的引用。

验收：

- 系统中不存在引用已永久删除原课的补课课节。
- 删除确认框明确显示关联补课数量和处理结果。
- 恢复原课及补课时能够重新建立一致关系。

## P1：保存性能与依赖安全

### [x] P1.1 合并高频输入产生的整库保存

完成记录：2026-07-13。vault mutation 改为 500ms 合并保存，页面隐藏、同步、退出和强制覆盖时刷新待保存内容。

问题：

- 课程内容、作业、备注、出勤备注等字段在每次输入时都会触发整份 vault 加密和上传。
- 长文本或慢网络下会积压大量加密任务和 PUT 请求。
- 会影响输入流畅度，并提高多设备版本冲突概率。

涉及文件：

- `src/frontend/App.tsx`
- `src/frontend/views/ScheduleView.tsx`
- `src/frontend/components/ScheduleLessonDetailPanel.tsx`
- `src/frontend/components/ScheduleLessonAttendancePanel.tsx`

建议方案：

- 页面内状态即时更新。
- 云端保存增加 300-800ms debounce 或 mutation 合并。
- 失焦、切换页面、退出和页面隐藏时强制刷新待保存内容。
- 保留明确的保存中、已保存和保存失败状态。

验收：

- 连续输入 100 个字符不会产生 100 次云端 PUT。
- 切换课程、切页或刷新前不会丢失最后输入内容。
- 多设备冲突提示仍然有效。
- 增加保存合并和强制刷新测试。

### [x] P1.2 处理 `xlsx` 已知高危漏洞

完成记录：2026-07-13。升级到 SheetJS 0.20.3，并增加文件数量、单文件大小、工作表、行数和单元格总量限制。

问题：

- 当前 `xlsx@0.18.5` 存在原型污染和 ReDoS 漏洞。
- npm 审计没有提供可自动安装的修复版本。
- Excel 文件属于用户可导入内容，风险高于纯开发依赖漏洞。

涉及文件：

- `package.json`
- `package-lock.json`
- `src/frontend/lib/scheduleImport.ts`

建议方案：

- 评估 SheetJS 官方维护版本、可信固定来源或替代解析库。
- 验证 `.xls` 和 `.xlsx` 两种校宝导出文件兼容性。
- 增加文件大小、工作表数量、行列数和单元格文本长度限制。
- 解析异常时提供明确提示，并避免阻塞主线程过久。

验收：

- 依赖审计不再报告当前两项 `xlsx` 高危漏洞，或形成明确的隔离和风险接受记录。
- 现有真实校宝 Excel 样本可以正常解析。
- 超大或异常工作簿会被安全拒绝。

### [x] P1.3 升级 Vite、Wrangler 及相关开发依赖

完成记录：2026-07-13。Vite 升级到 7.3.6、Wrangler 升级到 4.110.0、Workers 类型升级到 5.x；审计降至 0 项高危、1 项低危。

问题：

- `vite`、`wrangler`、`miniflare`、`undici`、`ws` 和 `esbuild` 当前存在已知漏洞。
- 多数属于开发或部署工具链，但仍应及时升级。

建议方案：

- 优先在现有主版本允许范围内升级。
- 主版本升级单独进行，避免和功能修改混合。
- 升级后重新运行构建、Worker 本地预览和部署验证。

验收：

- `npm audit` 高危项明显减少。
- 本地开发、构建和 Cloudflare 部署流程正常。

## P2：后端安全边界

### [ ] P2.1 为登录和账号接口增加频率限制

问题：

- 登录接口直接校验密码 verifier，没有账号或 IP 维度的失败次数限制。
- `auth/lookup`、登录和注册接口均可能被高频调用。

涉及文件：

- `src/worker/index.ts`
- `src/worker/router.ts`
- `migrations/`

建议方案：

- 按账号和来源 IP 记录短期失败次数。
- 增加指数退避、短期锁定和统一错误信息。
- 成功登录后清理失败计数。

验收：

- 连续失败达到阈值后返回 429。
- 正常登录和管理员操作不受明显影响。
- 增加 Worker 路由测试。

### [ ] P2.2 加密保存 AI 服务 API Key

问题：

- AI 服务 API Key 当前以普通 `TEXT` 字段保存在 D1。
- 页面展示虽然是掩码，但数据库数据泄漏会直接暴露密钥。

涉及文件：

- `migrations/0004_ai_provider_configs.sql`
- `src/worker/index.ts`
- `wrangler.toml`

建议方案：

- 使用 Worker Secret 派生 AES-GCM 密钥。
- D1 只保存密文、IV 和版本信息。
- 为已有明文记录提供一次性迁移。

验收：

- D1 中不再出现可直接使用的 API Key。
- AI 测试和正式调用能够正常解密使用。
- 管理页面仍只显示掩码。

### [ ] P2.3 限制加密档案和独立文档体积

问题：

- 主 vault 和通用加密文档上传目前只检查字段是否存在。
- 没有限制 payload 大小、文档键长度或每用户文档数量。

涉及文件：

- `src/worker/index.ts`
- `src/frontend/lib/storage.ts`

建议方案：

- 主 vault、教务核对历史和其他文档设置独立上限。
- 限制 `docType`、`docKey` 格式与长度。
- 返回可识别的容量错误，并在前端显示清理建议。

验收：

- 超限请求返回 413 或明确业务错误。
- 正常档案保存不受影响。
- 管理端能够查看用户档案估算体积。

### [ ] P2.4 减少每次 API 请求的 D1 清理写入

问题：

- 当前每个 API 请求都会清理过期会话。
- 每个非 GET 请求还会尝试执行到期账号删除。
- 项目已经有定时任务，重复执行会增加 D1 延迟和写入次数。

建议方案：

- 主要清理工作交给 Cloudflare Cron。
- 请求路径只做低频抽样或基于时间戳节流。
- 登录鉴权仍应即时拒绝已过期会话。

验收：

- 普通 API 请求不再固定执行全局 DELETE/CLEANUP。
- 定时清理和账号到期删除仍能可靠运行。

## P3：数据结构与兼容性

### [ ] P3.1 为解密后的 vault 增加运行时校验和迁移

问题：

- 解密结果目前直接断言为 `TeacherVault`。
- 损坏数据、旧版本字段或异常浏览器缓存可能在页面深处才报错。
- `vault.version` 已存在，但缺少集中迁移流程。

涉及文件：

- `src/frontend/lib/crypto.ts`
- `src/frontend/lib/storage.ts`
- `src/shared/types.ts`

建议方案：

- 增加运行时 schema 或可靠的手写校验器。
- 建立 `version -> version` 的迁移管线。
- 加载时生成诊断信息，并允许导出原始加密备份。

验收：

- 缺少可选字段的旧档案可以自动迁移。
- 核心数组或课节字段损坏时显示可理解错误，不出现白屏。
- 加入旧版本和损坏数据测试样本。

### [ ] P3.2 把引用完整性检查下沉到 mutation 层

问题：

- 学生、课程、校区删除是否允许，目前主要依赖前端按钮禁用。
- `App` 中的底层删除函数本身没有再次检查引用。
- 新入口或 AI 操作未来可能绕过 UI 保护。

建议方案：

- 在统一 mutation 层实现删除前检查。
- mutation 返回结构化结果：成功、被引用、关联数量和处理建议。
- UI 只负责展示，不作为唯一安全边界。

验收：

- 任意调用路径都无法删除仍被历史数据引用的核心档案。
- AI、批量操作和手动操作遵守同一规则。

## P4：自动测试与质量门禁

### [ ] P4.1 增加关键数据 mutation 测试

优先场景：

- 学习清单多条完成记录批量删除。
- 删除并恢复已完成补课。
- 删除原课及其关联补课。
- 批量更新课节和费用快照。
- 多个连续 `updateVault` 不相互覆盖。

### [ ] P4.2 增加 Worker 路由测试

优先场景：

- 登录限流和会话过期。
- vault 版本冲突。
- payload 体积限制。
- 管理员权限。
- AI 日限并发边界。
- 账号删除流程。

### [ ] P4.3 增加 React 组件和端到端测试

优先流程：

- 课程详情编辑开始/结束时间后统一保存。
- 学生缺勤、安排补课、完成补课、Excel 对账。
- 学生归档后重新导入历史月份 Excel。
- 回收站删除和恢复。
- 多设备版本冲突提示和处理。

建议工具：

- React Testing Library
- Playwright

### [ ] P4.4 增加 CI、lint 和格式检查

建议最低门禁：

- TypeScript 检查
- Vitest
- 生产构建
- ESLint 或 Biome
- 格式检查
- `git diff --check`
- `npm audit` 报告

验收：

- 每次提交或合并请求自动执行。
- P0/P1 关键回归测试失败时阻止合并。

## P5：结构与性能优化

### [ ] P5.1 拆分超大模块

建议顺序：

1. 从 `App.tsx` 抽出 vault store、保存队列和同步状态机。
2. 从 `ScheduleView.tsx` 抽出补课、回收站、排课模板和课程详情控制器。
3. 从 `StudentsView.tsx` 抽出课程档案、学生归档和设置管理。
4. 拆分 Worker 的 auth、vault、admin、AI 和账号删除路由。

验收：

- 单个模块职责清晰。
- mutation 逻辑可独立测试。
- 拆分前后行为保持一致。

### [ ] P5.2 统一重复工具函数

当前重复项包括：

- `daysBetween`
- `minutesForTime`
- `normalizeText`
- `dateWithWeekday`
- 时间重叠和日期解析包装

建议方案：

- 时间统一放入 `src/frontend/lib/time.ts`。
- 日期统一放入独立日期模块。
- 导入匹配文本归一化集中管理并补测试。

### [ ] P5.3 继续控制按需加载后的包体

当前主要构建产物：

- 主入口 JS：约 397KB，gzip 约 125KB
- `ScheduleView`：约 228KB，gzip 约 57KB
- `xlsx`：约 429KB，gzip 约 143KB，已按需加载

建议方案：

- 优先通过模块拆分和移除重复代码降低 `ScheduleView`。
- 保持 Excel、Markdown 和大型页面按需加载。
- 只有收益明确时再替换动画库。

## P6：项目文档维护

### [ ] P6.1 更新旧优化待办状态

问题：

- `项目优化调整待办.md` 中仍将以下项目标为未完成：
  - AI draft 应用逻辑抽取
  - vault mutation 工具抽取
  - 引入 Vitest
  - 工资、导入和解锁测试
- 当前代码已经部分或全部完成这些工作。

建议方案：

- 根据实际代码更新完成状态和日期。
- 后续以本文件作为新的执行主清单。
- 旧文档保留为历史记录并链接到本文件。

## 推荐执行顺序

### 第一阶段：防止数据不一致

1. P0.1 学习清单批量删除覆盖
2. P0.2 回收站恢复补课联动
3. P0.3 原课删除与补课引用
4. P4.1 对应 mutation 回归测试

### 第二阶段：降低保存压力和依赖风险

1. P1.1 高频保存合并
2. P1.2 `xlsx` 安全处理
3. P1.3 Vite/Wrangler 依赖升级

### 第三阶段：补后端安全边界

1. P2.1 登录限流
2. P2.2 AI Key 加密
3. P2.3 上传体积限制
4. P2.4 后端清理任务节流

### 第四阶段：建立长期质量保障

1. P3.1 vault schema 和迁移
2. P3.2 mutation 引用完整性
3. P4.2 Worker 测试
4. P4.3 组件和端到端测试
5. P4.4 CI、lint 和格式检查

### 第五阶段：结构收敛

1. P5.1 拆分超大模块
2. P5.2 统一重复工具函数
3. P5.3 包体持续优化
4. P6.1 更新历史文档

## 每项完成记录模板

```md
完成日期：YYYY-MM-DD
修改文件：
- `path/to/file`

实现说明：
- 

验证结果：
- `npm test`
- `npm run build`
- 相关手动场景

遗留风险：
- 无 / 具体说明
```
